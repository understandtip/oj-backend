# 线上配置文件
  
server:
  address: 0.0.0.0
  port: 8122
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_db?serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: d24bd93d438f30b6
  # Redis 配置
  # todo 需替换配置
  redis:
    port: 6379
    host: 127.0.0.1
    database: 4
  # Elasticsearch 配置
#  # todo 需替换配置
#  elasticsearch:
#    uris: http://localhost:9200
#    username: root
#    password: 123456
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''